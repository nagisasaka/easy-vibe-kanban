# Example only: replace OWNER and VERSION with an actually published image.
EVK_IMAGE=ghcr.io/OWNER/easy-vibe-kanban:VERSION
EVK_DOMAIN=evk.example.com
# Optional. DNS *.preview.example.com must point to this server; the TLS
# certificate must cover both EVK_DOMAIN and *.EVK_PREVIEW_DOMAIN.
EVK_PREVIEW_DOMAIN=preview.example.com
